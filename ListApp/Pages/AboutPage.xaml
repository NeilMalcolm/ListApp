<pages:BaseModalPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:ListApp.Pages"
             x:Class="ListApp.Pages.AboutPage">
    <ContentPage.Content>
        <StackLayout Padding="50">
            <Label Text="{Binding AboutUrl}"
                   Style="{StaticResource LinkLabelStyle}">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding OpenAboutUrlCommand}"/>
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>
    </ContentPage.Content>
</pages:BaseModalPage>